# PC ISO Build Workspace

This folder is reserved for generated build workspace files during local ISO preparation.

Expected generated items later:

- extracted or mounted base ISO workspace
- staged autoinstall seed
- staged FHSE shared bundle
- generated output ISO

