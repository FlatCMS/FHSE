# VM Appliance Build Workspace

This folder is reserved for generated build workspace files during local VM appliance preparation.

Expected generated items later:

- staged cloud-init seed
- staged FHSE shared bundle
- target image workspace
- generated VM output artifacts

