# VM ARM64 Autoinstall

This folder contains the Ubuntu Server ARM64 unattended installation seed used by the FHSE VM builder.

Injected into the ISO:

- `autoinstall/user-data`
- `autoinstall/meta-data`
